mut prev = 0;
mut curr = 1;
mut count = 0;
imut n = 10;

while (count < n) {
    imut val = curr;
    print(val);
    imut next = prev + curr;
    prev = curr;   
    curr = next;   
    count = count + 1;
}