#ifndef RESOLVE_H
#define RESOLVE_H

#include <stdio.h>

/**
 * Resolving values of different kinds 
 * ---------------- */

/** Resolves string value */
char* resolve_string(char* line, size_t* pos);

/** --------------------------- */
#endif
