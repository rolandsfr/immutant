fn pure is_palindrome(text) {
    mut i = 5;

    fn pure ret() {
        mut a = 20;
        return a + i;
    }
    
    return ret();
}

imut message = "racecar";
imut is_palindrome_result = is_palindrome(message);

print(is_palindrome_result);

