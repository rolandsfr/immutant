// Fibonacci sequence

fn fibonacci(n) {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

imut count = 15;
mut i = 0;

print("Fibonacci sequence:");
while (i < count) {
    imut fib_value = fibonacci(i);
    print(fib_value);
    i = i + 1;
}
